
import {
    init,
    SetModule,
    Service
} from "angular2-now";

export const moduleName = "progress";
export var service = {
    name: "ProgressService"
};
init();
SetModule(moduleName);
Service()
